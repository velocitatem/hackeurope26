# Core - always installed
python-dotenv
requests
tqdm

# Logging (lib)
python-logging-loki

# Backend (fastapi or flask)
fastapi
flask
flask-cors
uvicorn[standard]
pydantic

# Worker (Celery + Redis)
celery[redis]
redis
gitpython

# Scraper (lib)
seleniumbase
beautifulsoup4

# Minimal webapp
streamlit

# AI / Agent SDK
anthropic
claude-agent-sdk
kubernetes

# ML (heavy - comment out if not needed)
torch
tensorboard
